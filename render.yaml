services:
  - type: web
    name: renthub
    env: docker
    plan: free
    branch: main
    region: singapore
    dockerfilePath: ./Dockerfile
    buildCommand: ""
    startCommand: "php artisan serve --host=0.0.0.0 --port=80"
